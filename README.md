# barter
proxy codex as an openai-compatible api

barter is a full-featured openai-compatible api proxy that forwards requests to a standalone codex, allowing you to _barter_ codex usage that comes with a chatgpt plan for api-esque credits.
it supports the latest openai api spec, streaming, structured responses, and exposes `web_search` and `code_interpreter` as tools.
